<!DOCTYPE html>
<html lang="fr">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Infos utilisateur - RELIANCE ESCALADE</title>
  <script src="https://cdn.tailwindcss.com"></script>
  <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/4.0.0/css/all.min.css" rel="stylesheet">
  <script type="module" src="https://unpkg.com/ionicons@7.1.0/dist/ionicons/ionicons.esm.js"></script>
  <link href="/public/css/app.css" rel="stylesheet">
</head>
<body class="bg-gray-100 min-h-screen text-gray-900 flex">

  <!-- Sidebar -->
  <aside class="w-64 bg-gray-800 text-white border-r min-h-screen shadow-sm flex flex-col py-5 px-4">
    {{-- <div class="mb-8"> --}}
      {{-- <h2 class="flex items-center px-5 py-4 rounded-lg bg-blue-600 text-white font-semibold">
        Copyright RWA 2025
      </h2> --}}

       <!-- Logo -->
        {{-- <div class="flex items-center">
          <div class="gradient-primary p-2 rounded-lg shadow-md">
            <div class="w-8 h-8 bg-white rounded-sm flex items-center justify-center">
              <span class="text-blue-600 font-bold text-sm">RE</span>
            </div>
          </div>
          <div class="ml-4">
            <h1 class="text-xl font-bold text-gray-800">RELIANCE ESCALADE</h1>
            <p class="text-sm text-gray-600">Gestion des Ressources Humaines</p>
          </div>
        </div> --}}
    {{-- </div> --}}

    <nav class="flex flex-col space-y-2 text-md font-medium">
      <a href="{{ route('dashboard') }}" class="sidebar-link flex items-center px-5 py-4 rounded-lg hover:bg-gray-700 {{ request.url() === route('dashboard') ? 'bg-blue-700/90 text-white font-semibold' : '' }}">
        {{-- <i class="fas fa-th-large mr-3"></i> --}}
        <ion-icon name="logo-tableau" class="mr-3 text-2xl"></ion-icon> Dashboard
        <ion-icon name="chevron-down-outline" class="ml-auto text-xs"></ion-icon>
        {{-- <i class="fas fa-chevron-down ml-auto text-xs"></i> --}}
      </a>
      <a href="{{ route('posts.index') }}" class="sidebar-link flex items-center px-5 py-4 rounded-lg hover:bg-gray-700 {{ request.url().includes('/posts') ? 'bg-blue-600 text-white font-semibold' : '' }}">
        {{-- <i class="fas fa-briefcase mr-3"></i> --}}
        <ion-icon name="albums-outline" class="mr-3 text-2xl"></ion-icon>Postes
        <ion-icon name="chevron-forward-outline" class="ml-auto text-xs"></ion-icon>
      </a>
      <a href="{{ route('employees.index') }}" class="sidebar-link flex items-center px-5 py-4 rounded-lg hover:bg-gray-700 {{ request.url().includes('/employees') ? 'bg-blue-600 text-white font-semibold' : '' }}">
        {{-- <i class="fas fa-users mr-3"></i> --}}
        <ion-icon name="people-outline" class="mr-3 text-2xl"></ion-icon>Personnel
        <ion-icon name="chevron-forward-outline" class="ml-auto text-xs"></ion-icon>
      </a>
      <a href="{{ route('promotions.index') }}" class="sidebar-link flex items-center px-5 py-4 rounded-lg hover:bg-gray-700 {{ request.url().includes('/promotions') ? 'bg-blue-600 text-white font-semibold' : '' }}">
        {{-- <i class="fas fa-arrow-up mr-3"></i> --}}
        <ion-icon name="trending-up-outline" class="mr-3 text-2xl"></ion-icon>Promotions
        <ion-icon name="chevron-forward-outline" class="ml-auto text-xs"></ion-icon>
      </a>
      <a href="{{ route('demotions.index') }}" class="sidebar-link flex items-center px-5 py-4 rounded-lg hover:bg-gray-700 {{ request.url().includes('/demotions') ? 'bg-blue-600 text-white font-semibold' : '' }}">
        {{-- <i class="fas fa-arrow-down mr-3"></i> --}}
        <ion-icon name="trending-down-outline" class="mr-3 text-2xl"></ion-icon>Démotions
        <ion-icon name="chevron-forward-outline" class="ml-auto text-xs"></ion-icon>
      </a>
      <a href="{{ route('tracking.index') }}" class="sidebar-link flex items-center px-5 py-4 rounded-lg hover:bg-gray-700 {{ request.url().includes('/tracking') ? 'bg-blue-600 text-white font-semibold' : '' }}">
        {{-- <i class="fas fa-clone mr-3"></i> --}}
        <ion-icon name="pulse-outline" class="mr-3 text-2xl"></ion-icon> Suivi Employés
        <ion-icon name="chevron-forward-outline" class="ml-auto text-xs"></ion-icon>
      </a>
      <a href="{{ route('pay_slips.index') }}" class="sidebar-link flex items-center px-5 py-4 rounded-lg hover:bg-gray-700 {{ request.url().includes('/pay-slips') ? 'bg-blue-600 text-white font-semibold' : '' }}">
        {{-- <i class="fas fa-clone mr-3"></i> --}}
        <ion-icon name="receipt-outline" class="mr-3 text-2xl"></ion-icon> Fiches de Paie
        <ion-icon name="chevron-forward-outline" class="ml-auto text-xs"></ion-icon>
      </a>
      <a href="{{ route('orgcharts.index') }}" class="sidebar-link flex items-center px-5 py-4 rounded-lg hover:bg-gray-700 {{ request.url().includes('/orgcharts') ? 'bg-blue-600 text-white font-semibold' : '' }}">
        {{-- <i class="fas fa-clone mr-3"></i> --}}
        <ion-icon name="bonfire-outline" class="mr-3 text-2xl"></ion-icon> Organigramme
        <ion-icon name="chevron-forward-outline" class="ml-auto text-xs"></ion-icon>
      </a>
      <a href="{{ route('careers.index') }}" class="sidebar-link flex items-center px-5 py-4 rounded-lg hover:bg-gray-700 {{ request.url().includes('/careers') ? 'bg-blue-600 text-white font-semibold' : '' }}">
        {{-- <i class="fas fa-briefcase mr-3"></i> --}}
        <ion-icon name="logo-slack" class="mr-3 text-2xl"></ion-icon> Carrières
        <ion-icon name="chevron-forward-outline" class="ml-auto text-xs"></ion-icon>
      </a>
      <a href="{{ route('statistics.index') }}" class="sidebar-link flex items-center px-5 py-4 rounded-lg hover:bg-gray-700 {{ request.url().includes('/statistics') ? 'bg-blue-600 text-white font-semibold' : '' }}">
        {{-- <i class="fas fa-clone mr-3"></i> --}}
        <ion-icon name="bar-chart-outline" class="mr-3 text-2xl"></ion-icon> Statistiques
        <ion-icon name="chevron-forward-outline" class="ml-auto text-xs"></ion-icon>
      </a>
      @if(auth.user.role === 'Admin')
      <a href="{{ route('users.index') }}" class="sidebar-link flex items-center px-5 py-4 rounded-lg hover:bg-gray-700 {{ request.url().includes('/users') ? 'bg-blue-600 text-white font-semibold' : '' }}">
        {{-- <i class="fas fa-user-cog mr-3"></i> --}}
        <ion-icon name="finger-print-outline" class="mr-3 text-2xl"></ion-icon>Utilisateurs
        <ion-icon name="chevron-forward-outline" class="ml-auto text-xs"></ion-icon>
      </a>
      @endif
    </nav>
  </aside>

  <!-- Main Panel -->
  <div class="flex-1 flex flex-col">

    <!-- Header -->
    <header class="h-24 bg-white border-b px-6 flex items-center justify-between shadow-sm">
      <div>
        <div class="flex items-center">
          <div class="gradient-primary p-2 rounded-lg shadow-md">
            <div class="w-8 h-8 bg-white rounded-sm flex items-center justify-center">
              <span class="text-blue-600 font-bold text-sm">RE</span>
            </div>
          </div>
          <div class="ml-4">
            <h1 class="text-xl font-bold text-gray-800">RELIANCE ESCALADE</h1>
            <p class="text-sm text-gray-600">Gestion des Ressources Humaines</p>
          </div>
        </div>
      </div> <!-- Espace vide pour futur breadcrumb -->

      <div class="flex items-center space-x-6">
        <!-- Notifications -->
        {{-- <div class="relative">
          <i class="fas fa-bell text-gray-600 text-lg"></i>
          <span class="absolute -top-1 -right-2 bg-red-500 text-white text-xs font-bold px-1.5 py-0.5 rounded-full">3</span>
        </div> --}}

        <!-- User Menu -->
        <div class="flex items-center space-x-3">
          <div class="w-12 h-12 rounded-full bg-blue-600 text-white flex items-center justify-center text-sm shadow">
            {{-- <i class="fas fa-user"></i> --}}
            <ion-icon name="logo-octocat" class="text-3xl"></ion-icon>
          </div>
          <div class="text-md">
            <div class="font-semibold">{{ auth.user.fullName }}</div>
            <div class="text-xs text-gray-500 capitalize">{{ auth.user.role }}</div>
          </div>

          <form action="{{ route('auth.logout') }}" method="POST">
            {{ csrfField() }}
            <button type="submit" class="ml-3 text-red-600 hover:text-red-700 p-2 rounded-lg hover:bg-red-50 transition-all">
              <ion-icon name="log-out-outline" class="text-2xl"></ion-icon>
              {{-- <i class="fas fa-share-square"></i> --}}
            </button>
          </form>
        </div>
      </div>
    </header>

    <!-- Content Area -->
    <main class="p-6 overflow-y-auto flex-1 bg-gray-50">
      <!-- Flash Messages -->
      @if(flashMessages.has('success'))
        <div class="mb-6 bg-green-50 border border-green-200 text-green-700 px-4 py-3 rounded-lg fade-in">
          <i class="fas fa-check-circle mr-2"></i>
          {{ flashMessages.get('success') }}
        </div>
      @endif

      @if(flashMessages.has('error'))
        <div class="mb-6 bg-red-50 border border-red-200 text-red-700 px-4 py-3 rounded-lg fade-in">
          <i class="fas fa-exclamation-triangle mr-2"></i>
          {{ flashMessages.get('error') }}
        </div>
      @endif

      {{-- @section('title')
      Postes
      @endsection

      @section('content') --}}
      <!-- Premium User Header -->
      <div class="relative bg-gradient-to-br from-indigo-600/90 via-purple-700/90 to-pink-800/90 rounded-3xl overflow-hidden mb-8 shadow-2xl"
          style="background-image: url('https://images.pexels.com/photos/3184291/pexels-photo-3184291.jpeg?auto=compress&cs=tinysrgb&w=1260&h=750&dpr=1'); background-size: cover; background-position: center; background-blend-mode: overlay;">

        <!-- Animated Background Elements -->
        <div class="absolute inset-0">
          <div class="absolute top-0 right-0 w-96 h-96 bg-white/10 rounded-full blur-3xl animate-pulse"></div>
          <div class="absolute bottom-0 left-0 w-80 h-80 bg-indigo-300/20 rounded-full blur-2xl animate-bounce" style="animation-duration: 4s;"></div>
          <div class="absolute top-1/2 left-1/2 transform -translate-x-1/2 -translate-y-1/2 w-64 h-64 bg-purple-200/15 rounded-full blur-xl animate-pulse" style="animation-delay: 2s;"></div>
        </div>

        <div class="relative z-10 p-10">
          <div class="flex items-center justify-between">
            <div class="flex items-center gap-8">
              <!-- User Avatar -->
              <div class="relative">
                <div class="w-32 h-32 bg-white/20 backdrop-blur-lg rounded-full flex items-center justify-center shadow-2xl border-4 border-white/30">
                  <i class="fas fa-user text-white text-5xl drop-shadow-lg"></i>
                </div>
                <div class="absolute -bottom-2 -right-2 w-10 h-10 bg-{{ user.actif ? 'green' : 'red' }}-500 rounded-full flex items-center justify-center border-4 border-white shadow-lg">
                  <i class="fas fa-{{ user.actif ? 'check' : 'times' }} text-white text-sm"></i>
                </div>
                <!-- Role Badge -->
                <div class="absolute -top-2 -left-2 px-3 py-1 bg-{{
                  user.role === 'Admin' ? 'red' :
                  user.role === 'DG' ? 'yellow' :
                  'blue'
                }}-500 rounded-full text-white text-xs font-bold shadow-lg">
                  {{ user.role }}
                </div>
              </div>

              <div>
                <h1 class="text-4xl font-bold text-white mb-3 text-shadow-lg">
                  {{ user.fullName }}
                </h1>
                <p class="text-indigo-100 text-xl font-medium mb-2">{{ user.role }} - RELIANCE ESCALADE</p>
                <div class="flex items-center gap-6 mb-4">
                  <div class="flex items-center gap-2">
                    <i class="fas fa-envelope text-indigo-200"></i>
                    <span class="text-indigo-200">{{ user.email }}</span>
                  </div>
                  <div class="flex items-center gap-2">
                    <i class="fas fa-calendar text-indigo-200"></i>
                    <span class="text-indigo-200">Depuis {{ user.createdAt.toFormat('MMM yyyy') }}</span>
                  </div>
                </div>
                <div class="flex items-center gap-4">
                  <span class="px-4 py-2 bg-white/20 backdrop-blur-sm rounded-full text-white font-medium {{ user.actif ? 'border border-green-300' : 'border border-red-300' }}">
                    <i class="fas {{ user.actif ? 'fa-check-circle text-green-300' : 'fa-times-circle text-red-300' }} mr-2"></i>
                    {{ user.actif ? 'Compte Actif' : 'Compte Suspendu' }}
                  </span>
                  <span class="px-4 py-2 bg-white/20 backdrop-blur-sm rounded-full text-white font-medium border border-indigo-300">
                    <i class="fas fa-shield-alt text-indigo-300 mr-2"></i>
                    Accès Sécurisé
                  </span>
                </div>
              </div>
            </div>

            <!-- Action Buttons -->
            <div class="flex flex-col gap-3">
              <a href="{{ route('users.edit', [user.id]) }}" class="bg-white/20 hover:bg-white/30 backdrop-blur-sm text-white px-6 py-3 rounded-xl font-semibold transition-all duration-200 flex items-center gap-2 shadow-lg">
                <i class="fas fa-edit"></i>
                Modifier
              </a>
              @if(user.actif)
                <form action="{{ route('users.toggleStatus', [user.id]) }}" method="POST" class="inline">
                  {{ csrfField() }}
                  <button type="submit" class="bg-white/20 hover:bg-white/30 backdrop-blur-sm text-white px-6 py-3 rounded-xl font-semibold transition-all duration-200 flex items-center gap-2 shadow-lg w-full" onclick="return confirm('Suspendre ce compte ?')">
                    <i class="fas fa-pause"></i>
                    Suspendre
                  </button>
                </form>
              @else
                <form action="{{ route('users.toggleStatus', [user.id]) }}" method="POST" class="inline">
                  {{ csrfField() }}
                  <button type="submit" class="bg-white/20 hover:bg-white/30 backdrop-blur-sm text-white px-6 py-3 rounded-xl font-semibold transition-all duration-200 flex items-center gap-2 shadow-lg w-full" onclick="return confirm('Activer ce compte ?')">
                    <i class="fas fa-play"></i>
                    Activer
                  </button>
                </form>
              @endif
            </div>
          </div>
        </div>
      </div>

      <!-- User Details Grid -->
      <div class="grid grid-cols-1 lg:grid-cols-4 gap-8 mb-8">
        <!-- Account Information -->
        <div class="bg-white/95 backdrop-blur-sm rounded-2xl shadow-xl border border-gray-200/50 p-6 card-hover">
          <div class="flex items-center gap-4 mb-6">
            <div class="w-12 h-12 bg-gradient-to-r from-blue-500 to-blue-600 rounded-xl flex items-center justify-center shadow-lg">
              <i class="fas fa-id-card text-white text-xl"></i>
            </div>
            <h3 class="text-xl font-bold text-gray-800">Informations Compte</h3>
          </div>
          <div class="space-y-4">
            <div class="info-item">
              <label class="info-label">
                <i class="fas fa-envelope text-blue-500 mr-2"></i>
                Adresse Email
              </label>
              <p class="info-value">{{ user.email }}</p>
              <a href="mailto:{{ user.email }}" class="info-hint text-blue-600 hover:text-blue-700">Envoyer un email</a>
            </div>
            <div class="info-item">
              <label class="info-label">
                <i class="fas fa-calendar-plus text-blue-500 mr-2"></i>
                Date de Création
              </label>
              <p class="info-value">{{ user.createdAt.toFormat('dd/MM/yyyy') }}</p>
              <p class="info-hint">{{ user.createdAt.toRelative() }}</p>
            </div>
            <div class="info-item">
              <label class="info-label">
                <i class="fas fa-clock text-blue-500 mr-2"></i>
                Dernière Modification
              </label>
              <p class="info-value">{{ user.updatedAt ? user.updatedAt.toFormat('dd/MM/yyyy') : 'Jamais' }}</p>
              <p class="info-hint">{{ user.updatedAt ? user.updatedAt.toRelative() : 'Compte non modifié' }}</p>
            </div>
          </div>
        </div>

        <!-- Role and Permissions -->
        <div class="bg-white/95 backdrop-blur-sm rounded-2xl shadow-xl border border-gray-200/50 p-6 card-hover">
          <div class="flex items-center gap-4 mb-6">
            <div class="w-12 h-12 bg-gradient-to-r from-{{
              user.role === 'Admin' ? 'red-500 to-red-600' :
              user.role === 'DG' ? 'yellow-500 to-yellow-600' :
              'blue-500 to-blue-600'
            }} rounded-xl flex items-center justify-center shadow-lg">
              <i class="fas fa-{{
                user.role === 'Admin' ? 'crown' :
                user.role === 'DG' ? 'star' :
                'user-tie'
              }} text-white text-xl"></i>
            </div>
            <h3 class="text-xl font-bold text-gray-800">Rôle et Permissions</h3>
          </div>

          <div class="text-center mb-6">
            <div class="w-20 h-20 bg-{{
              user.role === 'Admin' ? 'red' :
              user.role === 'DG' ? 'yellow' :
              'blue'
            }}-500 rounded-full flex items-center justify-center mx-auto mb-4">
              <i class="fas fa-{{
                user.role === 'Admin' ? 'crown' :
                user.role === 'DG' ? 'star' :
                'user-tie'
              }} text-white text-2xl"></i>
            </div>
            <h4 class="text-lg font-bold text-gray-800">{{ user.role }}</h4>
            <p class="text-gray-600">{{
              user.role === 'Admin' ? 'Administrateur Système' :
              user.role === 'DG' ? 'Directeur Général' :
              'Directeur des Ressources Humaines'
            }}</p>
          </div>

          <div class="space-y-3">
            @if(user.role === 'Admin')
              <div class="permission-badge bg-red-50 text-red-700 border-red-200">
                <i class="fas fa-users-cog mr-2"></i>
                Gestion utilisateurs
              </div>
              <div class="permission-badge bg-red-50 text-red-700 border-red-200">
                <i class="fas fa-cogs mr-2"></i>
                Configuration système
              </div>
              <div class="permission-badge bg-red-50 text-red-700 border-red-200">
                <i class="fas fa-database mr-2"></i>
                Accès base de données
              </div>
            @elseif(user.role === 'DG')
              <div class="permission-badge bg-yellow-50 text-yellow-700 border-yellow-200">
                <i class="fas fa-chart-line mr-2"></i>
                Statistiques globales
              </div>
              <div class="permission-badge bg-yellow-50 text-yellow-700 border-yellow-200">
                <i class="fas fa-eye mr-2"></i>
                Vue d'ensemble
              </div>
              <div class="permission-badge bg-yellow-50 text-yellow-700 border-yellow-200">
                <i class="fas fa-check-circle mr-2"></i>
                Validation promotions
              </div>
            @else
              <div class="permission-badge bg-blue-50 text-blue-700 border-blue-200">
                <i class="fas fa-users mr-2"></i>
                Gestion employés
              </div>
              <div class="permission-badge bg-blue-50 text-blue-700 border-blue-200">
                <i class="fas fa-arrow-up mr-2"></i>
                Promotions/Démotions
              </div>
              <div class="permission-badge bg-blue-50 text-blue-700 border-blue-200">
                <i class="fas fa-file-invoice mr-2"></i>
                Fiches de paie
              </div>
            @endif
          </div>
        </div>

        <!-- Security Information -->
        <div class="bg-white/95 backdrop-blur-sm rounded-2xl shadow-xl border border-gray-200/50 p-6 card-hover">
          <div class="flex items-center gap-4 mb-6">
            <div class="w-12 h-12 bg-gradient-to-r from-green-500 to-green-600 rounded-xl flex items-center justify-center shadow-lg">
              <i class="fas fa-shield-alt text-white text-xl"></i>
            </div>
            <h3 class="text-xl font-bold text-gray-800">Sécurité</h3>
          </div>

          <div class="space-y-4">
            <div class="security-item">
              <div class="flex justify-between items-center mb-2">
                <span class="text-sm font-medium text-gray-600">Statut du Compte</span>
                <span class="text-lg font-bold {{ user.actif ? 'text-green-600' : 'text-red-600' }}">
                  {{ user.actif ? 'Actif' : 'Suspendu' }}
                </span>
              </div>
              <div class="w-full bg-gray-200 rounded-full h-2">
                <div class="bg-{{ user.actif ? 'green' : 'red' }}-500 h-2 rounded-full" style="width: {{ user.actif ? '100' : '0' }}%"></div>
              </div>
            </div>

            <div class="security-item">
              <div class="flex justify-between items-center mb-2">
                <span class="text-sm font-medium text-gray-600">Niveau de Sécurité</span>
                <span class="text-lg font-bold text-green-600">Élevé</span>
              </div>
              <div class="w-full bg-gray-200 rounded-full h-2">
                <div class="bg-green-500 h-2 rounded-full" style="width: 90%"></div>
              </div>
            </div>

            <div class="security-item">
              <div class="flex justify-between items-center mb-2">
                <span class="text-sm font-medium text-gray-600">Dernière Connexion</span>
                <span class="text-sm font-medium text-gray-800">Aujourd'hui</span>
              </div>
              <div class="text-xs text-gray-500">IP: 192.168.1.100</div>
            </div>
          </div>
        </div>

        <!-- Activity Summary -->
        <div class="bg-white/95 backdrop-blur-sm rounded-2xl shadow-xl border border-gray-200/50 p-6 card-hover">
          <div class="flex items-center gap-4 mb-6">
            <div class="w-12 h-12 bg-gradient-to-r from-purple-500 to-purple-600 rounded-xl flex items-center justify-center shadow-lg">
              <i class="fas fa-chart-bar text-white text-xl"></i>
            </div>
            <h3 class="text-xl font-bold text-gray-800">Activité</h3>
          </div>

          <div class="grid grid-cols-2 gap-4">
            <div class="stat-item">
              <div class="stat-number text-blue-600">24</div>
              <div class="stat-label">Connexions</div>
            </div>
            <div class="stat-item">
              <div class="stat-number text-green-600">156</div>
              <div class="stat-label">Actions</div>
            </div>
            <div class="stat-item">
              <div class="stat-number text-purple-600">12</div>
              <div class="stat-label">Modifications</div>
            </div>
            <div class="stat-item">
              <div class="stat-number text-orange-600">3</div>
              <div class="stat-label">Rapports</div>
            </div>
          </div>
        </div>
      </div>

      <!-- User Activity Tabs -->
      <div class="bg-white/95 backdrop-blur-sm rounded-3xl shadow-2xl border border-gray-200/50 overflow-hidden">
        <!-- Tab Navigation -->
        <div class="border-b border-gray-200/50 bg-gradient-to-r from-gray-50 to-white">
          <nav class="flex space-x-0" aria-label="Tabs">
            <button class="premium-tab-button active" data-tab="activity">
              <div class="tab-icon bg-blue-100 text-blue-600">
                <i class="fas fa-history"></i>
              </div>
              <div class="tab-content">
                <div class="tab-title">Activité Récente</div>
                <div class="tab-subtitle">Dernières actions</div>
              </div>
            </button>

            <button class="premium-tab-button" data-tab="permissions">
              <div class="tab-icon bg-purple-100 text-purple-600">
                <i class="fas fa-key"></i>
              </div>
              <div class="tab-content">
                <div class="tab-title">Permissions</div>
                <div class="tab-subtitle">Droits d'accès</div>
              </div>
            </button>

            <button class="premium-tab-button" data-tab="sessions">
              <div class="tab-icon bg-green-100 text-green-600">
                <i class="fas fa-desktop"></i>
              </div>
              <div class="tab-content">
                <div class="tab-title">Sessions</div>
                <div class="tab-subtitle">Connexions actives</div>
              </div>
            </button>

            <button class="premium-tab-button" data-tab="security">
              <div class="tab-icon bg-red-100 text-red-600">
                <i class="fas fa-shield-alt"></i>
              </div>
              <div class="tab-content">
                <div class="tab-title">Sécurité</div>
                <div class="tab-subtitle">Paramètres sécurité</div>
              </div>
            </button>
          </nav>
        </div>

        <!-- Tab Content -->
        <div class="p-8">
          <!-- Activity Tab -->
          <div id="activity-tab" class="premium-tab-content active">
            <div class="flex items-center justify-between mb-6">
              <h3 class="text-2xl font-bold text-gray-800">Activité Récente</h3>
              <button onclick="refreshActivity()" class="btn-outline">
                <i class="fas fa-sync-alt mr-2"></i>
                Actualiser
              </button>
            </div>

            <!-- Activity Timeline -->
            <div class="activity-timeline">
              <div class="timeline-item">
                <div class="timeline-marker bg-green-500">
                  <i class="fas fa-sign-in-alt text-white"></i>
                </div>
                <div class="timeline-content">
                  <h4 class="timeline-title">Connexion au système</h4>
                  <p class="timeline-time">Aujourd'hui à 08:30</p>
                  <p class="timeline-description">Connexion depuis l'adresse IP 192.168.1.100</p>
                </div>
              </div>

              <div class="timeline-item">
                <div class="timeline-marker bg-blue-500">
                  <i class="fas fa-edit text-white"></i>
                </div>
                <div class="timeline-content">
                  <h4 class="timeline-title">Modification employé</h4>
                  <p class="timeline-time">Hier à 16:45</p>
                  <p class="timeline-description">Mise à jour du profil de Keli Princesse DOH</p>
                </div>
              </div>

              <div class="timeline-item">
                <div class="timeline-marker bg-purple-500">
                  <i class="fas fa-file-invoice text-white"></i>
                </div>
                <div class="timeline-content">
                  <h4 class="timeline-title">Génération fiche de paie</h4>
                  <p class="timeline-time">Hier à 14:20</p>
                  <p class="timeline-description">Fiche de paie générée pour Janvier 2025</p>
                </div>
              </div>
            </div>
          </div>

          <!-- Permissions Tab -->
          <div id="permissions-tab" class="premium-tab-content">
            <div class="flex items-center justify-between mb-6">
              <h3 class="text-2xl font-bold text-gray-800">Permissions Détaillées</h3>
            </div>

            <div class="grid grid-cols-1 md:grid-cols-2 gap-6">
              <!-- Module Permissions -->
              <div class="permission-module">
                <h4 class="module-title">
                  <i class="fas fa-users text-blue-600 mr-2"></i>
                  Gestion des Employés
                </h4>
                <div class="permission-list">
                  <div class="permission-item {{ user.role === 'DRH' || user.role === 'Admin' ? 'granted' : 'denied' }}">
                    <i class="fas fa-{{ user.role === 'DRH' || user.role === 'Admin' ? 'check' : 'times' }} mr-2"></i>
                    Créer employés
                  </div>
                  <div class="permission-item {{ user.role === 'DRH' || user.role === 'Admin' ? 'granted' : 'denied' }}">
                    <i class="fas fa-{{ user.role === 'DRH' || user.role === 'Admin' ? 'check' : 'times' }} mr-2"></i>
                    Modifier employés
                  </div>
                  <div class="permission-item granted">
                    <i class="fas fa-check mr-2"></i>
                    Voir employés
                  </div>
                </div>
              </div>

              <div class="permission-module">
                <h4 class="module-title">
                  <i class="fas fa-chart-bar text-purple-600 mr-2"></i>
                  Statistiques
                </h4>
                <div class="permission-list">
                  <div class="permission-item granted">
                    <i class="fas fa-check mr-2"></i>
                    Voir statistiques
                  </div>
                  <div class="permission-item {{ user.role === 'Admin' ? 'granted' : 'denied' }}">
                    <i class="fas fa-{{ user.role === 'Admin' ? 'check' : 'times' }} mr-2"></i>
                    Exporter données
                  </div>
                </div>
              </div>
            </div>
          </div>

          <!-- Sessions Tab -->
          <div id="sessions-tab" class="premium-tab-content">
            <div class="flex items-center justify-between mb-6">
              <h3 class="text-2xl font-bold text-gray-800">Sessions Actives</h3>
              <button onclick="revokeAllSessions()" class="btn-danger">
                <i class="fas fa-sign-out-alt mr-2"></i>
                Déconnecter Tout
              </button>
            </div>

            <div class="space-y-4">
              <div class="session-item">
                <div class="flex items-center justify-between">
                  <div class="flex items-center gap-4">
                    <div class="w-12 h-12 bg-green-100 rounded-full flex items-center justify-center">
                      <i class="fas fa-desktop text-green-600"></i>
                    </div>
                    <div>
                      <h4 class="font-semibold text-gray-800">Session Actuelle</h4>
                      <p class="text-sm text-gray-600">Windows 11 - Chrome 120</p>
                      <p class="text-xs text-gray-500">IP: 192.168.1.100 - Lomé, Togo</p>
                    </div>
                  </div>
                  <div class="text-right">
                    <div class="text-sm font-medium text-green-600">Actif</div>
                    <div class="text-xs text-gray-500">Depuis 2h 30m</div>
                  </div>
                </div>
              </div>
            </div>
          </div>

          <!-- Security Tab -->
          <div id="security-tab" class="premium-tab-content">
            <div class="flex items-center justify-between mb-6">
              <h3 class="text-2xl font-bold text-gray-800">Paramètres de Sécurité</h3>
            </div>

            <div class="grid grid-cols-1 md:grid-cols-2 gap-6">
              <div class="security-setting">
                <h4 class="setting-title">
                  <i class="fas fa-lock text-blue-600 mr-2"></i>
                  Mot de Passe
                </h4>
                <div class="setting-content">
                  <p class="text-sm text-gray-600 mb-3">Dernière modification: {{ user.updatedAt ? user.updatedAt.toFormat('dd/MM/yyyy') : 'Jamais' }}</p>
                  <button onclick="resetPassword()" class="btn-outline-sm">
                    <i class="fas fa-key mr-2"></i>
                    Réinitialiser
                  </button>
                </div>
              </div>

              <div class="security-setting">
                <h4 class="setting-title">
                  <i class="fas fa-history text-purple-600 mr-2"></i>
                  Historique de Connexion
                </h4>
                <div class="setting-content">
                  <p class="text-sm text-gray-600 mb-3">24 connexions ce mois</p>
                  <button onclick="viewLoginHistory()" class="btn-outline-sm">
                    <i class="fas fa-eye mr-2"></i>
                    Voir Historique
                  </button>
                </div>
              </div>
            </div>
          </div>
        </div>
      </div>

      <style>
      /* Premium User Show Page Styles */

      /* Info Items */
      .info-item {
        @apply p-4 bg-gray-50/50 rounded-xl border border-gray-100 transition-all duration-200 hover:bg-gray-50 hover:shadow-sm;
      }

      .info-label {
        @apply block text-sm font-semibold text-gray-600 mb-2 flex items-center;
      }

      .info-value {
        @apply text-base font-medium text-gray-800 mb-1;
      }

      .info-hint {
        @apply text-xs text-gray-500 italic;
      }

      /* Stats */
      .stat-item {
        @apply text-center p-3 bg-gray-50/50 rounded-xl border border-gray-100;
      }

      .stat-number {
        @apply text-2xl font-bold mb-1;
      }

      .stat-label {
        @apply text-xs text-gray-600 font-medium;
      }

      /* Security Items */
      .security-item {
        @apply p-3 bg-gray-50/50 rounded-lg;
      }

      /* Permission Badges */
      .permission-badge {
        @apply px-3 py-2 rounded-lg text-sm font-medium border flex items-center;
      }

      /* Premium Tabs */
      .premium-tab-button {
        @apply flex items-center gap-4 px-6 py-4 border-b-3 border-transparent text-gray-600 hover:text-gray-800 hover:bg-gray-50/50 transition-all duration-200 cursor-pointer min-w-0 flex-1;
      }

      .premium-tab-button.active {
        @apply border-indigo-500 text-indigo-600 bg-indigo-50/50;
      }

      .tab-icon {
        @apply w-12 h-12 rounded-xl flex items-center justify-center flex-shrink-0;
      }

      .tab-content {
        @apply min-w-0;
      }

      .tab-title {
        @apply font-semibold text-sm;
      }

      .tab-subtitle {
        @apply text-xs text-gray-500 mt-1;
      }

      .premium-tab-content {
        @apply hidden;
      }

      .premium-tab-content.active {
        @apply block;
      }

      /* Activity Timeline */
      .activity-timeline {
        @apply space-y-6;
      }

      .timeline-item {
        @apply flex items-start gap-4;
      }

      .timeline-marker {
        @apply w-10 h-10 rounded-full flex items-center justify-center flex-shrink-0;
      }

      .timeline-content {
        @apply flex-1 bg-gray-50 rounded-xl p-4;
      }

      .timeline-title {
        @apply font-semibold text-gray-800 mb-1;
      }

      .timeline-time {
        @apply text-sm text-gray-600 mb-2;
      }

      .timeline-description {
        @apply text-sm text-gray-700;
      }

      /* Permission Modules */
      .permission-module {
        @apply bg-gray-50 rounded-xl p-6 border border-gray-200;
      }

      .module-title {
        @apply font-semibold text-gray-800 mb-4 flex items-center;
      }

      .permission-list {
        @apply space-y-2;
      }

      .permission-item {
        @apply text-sm flex items-center;
      }

      .permission-item.granted {
        @apply text-green-700;
      }

      .permission-item.denied {
        @apply text-red-700;
      }

      /* Session Items */
      .session-item {
        @apply bg-gray-50 rounded-xl p-4 border border-gray-200;
      }

      /* Security Settings */
      .security-setting {
        @apply bg-gray-50 rounded-xl p-6 border border-gray-200;
      }

      .setting-title {
        @apply font-semibold text-gray-800 mb-3 flex items-center;
      }

      .setting-content {
        @apply space-y-2;
      }

      /* Buttons */
      .btn-outline {
        @apply px-4 py-2 border border-gray-300 rounded-lg text-sm font-medium text-gray-700 hover:bg-gray-50 transition-all duration-200 flex items-center;
      }

      .btn-outline-sm {
        @apply px-3 py-2 border border-gray-300 rounded-lg text-xs font-medium text-gray-700 hover:bg-gray-50 transition-all duration-200 flex items-center;
      }

      .btn-danger {
        @apply bg-gradient-to-r from-red-600 to-red-700 hover:from-red-700 hover:to-red-800 text-white font-semibold py-2 px-4 rounded-lg transition-all duration-200 transform hover:scale-105 shadow-lg flex items-center text-sm;
      }

      /* Card Hover Effects */
      .card-hover {
        @apply transition-all duration-300;
      }

      .card-hover:hover {
        @apply transform translateY(-2px) shadow-xl;
      }

      /* Responsive Design */
      @media (max-width: 768px) {
        .premium-tab-button {
          @apply flex-col gap-2 px-4 py-3;
        }

        .tab-icon {
          @apply w-8 h-8;
        }

        .tab-title {
          @apply text-xs;
        }

        .tab-subtitle {
          @apply hidden;
        }
      }
      </style>

      <script>
      // Tab functionality
      document.querySelectorAll('.premium-tab-button').forEach(button => {
        button.addEventListener('click', () => {
          // Remove active class from all buttons and contents
          document.querySelectorAll('.premium-tab-button').forEach(b => b.classList.remove('active'));
          document.querySelectorAll('.premium-tab-content').forEach(c => c.classList.remove('active'));

          // Add active class to clicked button
          button.classList.add('active');

          // Show corresponding content
          const tabId = button.getAttribute('data-tab') + '-tab';
          document.getElementById(tabId)?.classList.add('active');
        });
      });

      // Security functions
      function resetPassword() {
        if (confirm('Réinitialiser le mot de passe de cet utilisateur ?')) {
          // Implementation would handle password reset
          alert('Un email de réinitialisation sera envoyé à l\'utilisateur.');
        }
      }

      function revokeAllSessions() {
        if (confirm('Déconnecter toutes les sessions de cet utilisateur ?')) {
          // Implementation would revoke all sessions
          alert('Toutes les sessions ont été révoquées.');
        }
      }

      function refreshActivity() {
        // Refresh activity timeline
        location.reload();
      }

      function viewLoginHistory() {
        // Open login history modal or page
        alert('Historique de connexion - Fonctionnalité à implémenter');
      }

      // Initialize
      document.addEventListener('DOMContentLoaded', function() {
        // Add smooth scrolling for internal links
        document.querySelectorAll('a[href^="#"]').forEach(anchor => {
          anchor.addEventListener('click', function (e) {
            e.preventDefault();
            const target = document.querySelector(this.getAttribute('href'));
            if (target) {
              target.scrollIntoView({
                behavior: 'smooth',
                block: 'start'
              });
            }
          });
        });
      });
      </script>
      {{-- @endsection --}}
      </main>
  </div>
</body>
</html>
