<!DOCTYPE html>
<html lang="fr">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Code d'Accès - RELIANCE ESCALADE</title>
  <script src="https://cdn.tailwindcss.com"></script>
  <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css" rel="stylesheet">
  <link href="/css/app.css" rel="stylesheet">
</head>
<body class="min-h-screen bg-gradient-to-br from-gray-300 via-purple-50 to-slate-200 flex items-center justify-center relative overflow-hidden">
  <!-- Background decorative elements -->
  <div class="absolute top-0 left-0 w-full h-full">
    <div class="absolute top-32 right-32 w-96 h-96 bg-blue-500/10 rounded-full blur-3xl"></div>
    <div class="absolute bottom-32 left-32 w-80 h-80 bg-purple-500/10 rounded-full blur-3xl"></div>
    <div class="absolute top-1/2 left-1/2 transform -translate-x-1/2 -translate-y-1/2 w-64 h-64 bg-white/5 rounded-full blur-2xl"></div>
  </div>

  <div class="relative z-10 max-w-md w-full mx-4 fade-in">
    <div class="bg-gradient-to-br from-slate-900 via-blue-900 to-slate-900 overflow-hidden rounded-3xl shadow-2xl p-10 border border-white/20">
      <!-- Header -->
      <div class="text-center mb-8">
        <div class="flex justify-center mb-6">
          <div class="gradient-bg p-6 rounded-2xl shadow-lg">
            <i class="fas fa-shield-alt text-white text-5xl"></i>
          </div>
        </div>
        <h1 class="text-3xl font-bold text-white mb-2 text-shadow">RELIANCE ESCALADE</h1>
        <p class="text-blue-100 text-lg">Accès Sécurisé</p>
        <div class="h-1 w-32 bg-gradient-to-r from-blue-400 to-purple-400 mx-auto mt-4 rounded-full"></div>
      </div>

      <!-- Flash Messages -->
      @if(flashMessages.has('error'))
        <div class="mb-6 bg-red-500/20 border border-red-400/30 text-red-100 px-4 py-3 rounded-lg backdrop-blur-sm">
          <i class="fas fa-exclamation-triangle mr-2"></i>
          {{ flashMessages.get('error') }}
        </div>
      @endif

      <!-- Form -->
      <form action="{{ route('auth.verify_access') }}" method="POST" class="space-y-6">
        {{ csrfField() }}

        <div>
          <label for="access_code" class="block text-sm font-medium text-blue-100 mb-3">
            <i class="fas fa-key mr-2"></i>
            Code d'Accès Interne
          </label>
          <div class="relative">
            <input
              id="access_code"
              name="access_code"
              type="password"
              class="w-full bg-white/10 border border-white/20 rounded-xl px-4 py-4 text-white placeholder-blue-200 focus:outline-none focus:border-blue-400 focus:ring-2 focus:ring-blue-400/50 transition-all backdrop-blur-sm"
              placeholder="Entrez le code d'accès sécurisé"
              required
              autocomplete="off"
            />
            <div class="absolute inset-y-0 right-0 pr-4 flex items-center">
              <i class="fas fa-lock text-blue-300"></i>
            </div>
          </div>
          <p class="text-xs text-blue-200 mt-2 opacity-75">
            <i class="fas fa-info-circle mr-1"></i>
            Contactez l'administrateur système pour obtenir le code d'accès
          </p>
        </div>

        <button
          type="submit"
          class="w-full gradient-bg hover:opacity-90 text-white font-semibold py-4 px-6 rounded-xl transition-all duration-300 transform hover:scale-105 flex items-center justify-center gap-3 shadow-lg"
        >
          <i class="fas fa-arrow-right"></i>
          Vérifier le Code d'Accès
        </button>
      </form>

      <!-- Info -->
      <div class="mt-8 pt-6 border-t border-white/10">
        <div class="text-center">
          <p class="text-xs text-blue-200 opacity-75 mb-2">
            Système de Gestion des Ressources Humaines
          </p>
          <p class="text-xs text-blue-300">
            © 2025 RELIANCE WEST AFRICA - Tous droits réservés
          </p>
        </div>
      </div>
    </div>
  </div>
</body>
</html>
