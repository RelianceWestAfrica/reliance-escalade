<!DOCTYPE html>
<html lang="fr">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Nouveau fiche de paie - RELIANCE ESCALADE</title>
  <script src="https://cdn.tailwindcss.com"></script>
  <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/4.0.0/css/all.min.css" rel="stylesheet">
  <script type="module" src="https://unpkg.com/ionicons@7.1.0/dist/ionicons/ionicons.esm.js"></script>
  <link href="/public/css/app.css" rel="stylesheet">
</head>
<body class="bg-gray-100 min-h-screen text-gray-900 flex">

  <!-- Sidebar -->
  <aside class="w-64 bg-gray-800 text-white border-r min-h-screen shadow-sm flex flex-col py-5 px-4">
    {{-- <div class="mb-8"> --}}
      {{-- <h2 class="flex items-center px-5 py-4 rounded-lg bg-blue-600 text-white font-semibold">
        Copyright RWA 2025
      </h2> --}}

       <!-- Logo -->
        {{-- <div class="flex items-center">
          <div class="gradient-primary p-2 rounded-lg shadow-md">
            <div class="w-8 h-8 bg-white rounded-sm flex items-center justify-center">
              <span class="text-blue-600 font-bold text-sm">RE</span>
            </div>
          </div>
          <div class="ml-4">
            <h1 class="text-xl font-bold text-gray-800">RELIANCE ESCALADE</h1>
            <p class="text-sm text-gray-600">Gestion des Ressources Humaines</p>
          </div>
        </div> --}}
    {{-- </div> --}}

    <nav class="flex flex-col space-y-2 text-md font-medium">
      <a href="{{ route('dashboard') }}" class="sidebar-link flex items-center px-5 py-4 rounded-lg hover:bg-gray-700 {{ request.url() === route('dashboard') ? 'bg-blue-700/90 text-white font-semibold' : '' }}">
        {{-- <i class="fas fa-th-large mr-3"></i> --}}
        <ion-icon name="logo-tableau" class="mr-3 text-2xl"></ion-icon> Dashboard
        <ion-icon name="chevron-down-outline" class="ml-auto text-xs"></ion-icon>
        {{-- <i class="fas fa-chevron-down ml-auto text-xs"></i> --}}
      </a>
      <a href="{{ route('posts.index') }}" class="sidebar-link flex items-center px-5 py-4 rounded-lg hover:bg-gray-700 {{ request.url().includes('/posts') ? 'bg-blue-600 text-white font-semibold' : '' }}">
        {{-- <i class="fas fa-briefcase mr-3"></i> --}}
        <ion-icon name="albums-outline" class="mr-3 text-2xl"></ion-icon>Postes
        <ion-icon name="chevron-forward-outline" class="ml-auto text-xs"></ion-icon>
      </a>
      <a href="{{ route('employees.index') }}" class="sidebar-link flex items-center px-5 py-4 rounded-lg hover:bg-gray-700 {{ request.url().includes('/employees') ? 'bg-blue-600 text-white font-semibold' : '' }}">
        {{-- <i class="fas fa-users mr-3"></i> --}}
        <ion-icon name="people-outline" class="mr-3 text-2xl"></ion-icon>Personnel
        <ion-icon name="chevron-forward-outline" class="ml-auto text-xs"></ion-icon>
      </a>
      <a href="{{ route('promotions.index') }}" class="sidebar-link flex items-center px-5 py-4 rounded-lg hover:bg-gray-700 {{ request.url().includes('/promotions') ? 'bg-blue-600 text-white font-semibold' : '' }}">
        {{-- <i class="fas fa-arrow-up mr-3"></i> --}}
        <ion-icon name="trending-up-outline" class="mr-3 text-2xl"></ion-icon>Promotions
        <ion-icon name="chevron-forward-outline" class="ml-auto text-xs"></ion-icon>
      </a>
      <a href="{{ route('demotions.index') }}" class="sidebar-link flex items-center px-5 py-4 rounded-lg hover:bg-gray-700 {{ request.url().includes('/demotions') ? 'bg-blue-600 text-white font-semibold' : '' }}">
        {{-- <i class="fas fa-arrow-down mr-3"></i> --}}
        <ion-icon name="trending-down-outline" class="mr-3 text-2xl"></ion-icon>Démotions
        <ion-icon name="chevron-forward-outline" class="ml-auto text-xs"></ion-icon>
      </a>
      <a href="{{ route('tracking.index') }}" class="sidebar-link flex items-center px-5 py-4 rounded-lg hover:bg-gray-700 {{ request.url().includes('/tracking') ? 'bg-blue-600 text-white font-semibold' : '' }}">
        {{-- <i class="fas fa-clone mr-3"></i> --}}
        <ion-icon name="pulse-outline" class="mr-3 text-2xl"></ion-icon> Suivi Employés
        <ion-icon name="chevron-forward-outline" class="ml-auto text-xs"></ion-icon>
      </a>
      <a href="{{ route('pay_slips.index') }}" class="sidebar-link flex items-center px-5 py-4 rounded-lg hover:bg-gray-700 {{ request.url().includes('/pay-slips') ? 'bg-blue-600 text-white font-semibold' : '' }}">
        {{-- <i class="fas fa-clone mr-3"></i> --}}
        <ion-icon name="receipt-outline" class="mr-3 text-2xl"></ion-icon> Fiches de Paie
        <ion-icon name="chevron-forward-outline" class="ml-auto text-xs"></ion-icon>
      </a>
      {{-- <a href="{{ route('orgcharts.index') }}" class="sidebar-link flex items-center px-5 py-4 rounded-lg hover:bg-gray-700 {{ request.url().includes('/orgcharts') ? 'bg-blue-600 text-white font-semibold' : '' }}">
        {{-- <i class="fas fa-clone mr-3"></i> --}}
        <ion-icon name="bonfire-outline" class="mr-3 text-2xl"></ion-icon> Organigramme
        <ion-icon name="chevron-forward-outline" class="ml-auto text-xs"></ion-icon>
      </a> --}}
      <a href="{{ route('careers.index') }}" class="sidebar-link flex items-center px-5 py-4 rounded-lg hover:bg-gray-700 {{ request.url().includes('/careers') ? 'bg-blue-600 text-white font-semibold' : '' }}">
        {{-- <i class="fas fa-briefcase mr-3"></i> --}}
        <ion-icon name="logo-slack" class="mr-3 text-2xl"></ion-icon> Carrières
        <ion-icon name="chevron-forward-outline" class="ml-auto text-xs"></ion-icon>
      </a>
      <a href="{{ route('statistics.index') }}" class="sidebar-link flex items-center px-5 py-4 rounded-lg hover:bg-gray-700 {{ request.url().includes('/statistics') ? 'bg-blue-600 text-white font-semibold' : '' }}">
        {{-- <i class="fas fa-clone mr-3"></i> --}}
        <ion-icon name="bar-chart-outline" class="mr-3 text-2xl"></ion-icon> Statistiques
        <ion-icon name="chevron-forward-outline" class="ml-auto text-xs"></ion-icon>
      </a>
      @if(auth.user.role === 'Admin')
        <a href="{{ route('rwa_countries.index') }}" class="sidebar-link flex items-center px-5 py-4 rounded-lg hover:bg-gray-700 {{ request.url().includes('/rwa-countries') ? 'bg-blue-600 text-white font-semibold' : '' }}">
          <ion-icon name="earth-outline" class="mr-3 text-2xl"></ion-icon>Instances RWA
          <ion-icon name="chevron-forward-outline" class="ml-auto text-xs"></ion-icon>
        </a>
      @endif
      @if(auth.user.role === 'Admin')
      <a href="{{ route('users.index') }}" class="sidebar-link flex items-center px-5 py-4 rounded-lg hover:bg-gray-700 {{ request.url().includes('/users') ? 'bg-blue-600 text-white font-semibold' : '' }}">
        {{-- <i class="fas fa-user-cog mr-3"></i> --}}
        <ion-icon name="finger-print-outline" class="mr-3 text-2xl"></ion-icon>Utilisateurs
        <ion-icon name="chevron-forward-outline" class="ml-auto text-xs"></ion-icon>
      </a>
      @endif
    </nav>
  </aside>

  <!-- Main Panel -->
  <div class="flex-1 flex flex-col">

    <!-- Header -->
    <header class="h-24 bg-white border-b px-6 flex items-center justify-between shadow-sm">
      <div>
        <div class="flex items-center">
          <div class="gradient-primary p-2 rounded-lg shadow-md">
            <div class="w-8 h-8 bg-white rounded-sm flex items-center justify-center">
              <span class="text-blue-600 font-bold text-sm">RE</span>
            </div>
          </div>
          <div class="ml-4">
            <h1 class="text-xl font-bold text-gray-800"style="text-transform: uppercase;">RELIANCE ESCALADE {{instanceCountry}}</h1>
            <p class="text-sm text-gray-600">Gestion des Ressources Humaines</p>
          </div>
        </div>
      </div> <!-- Espace vide pour futur breadcrumb -->

      <div class="flex items-center space-x-6">
        <!-- Notifications -->
        {{-- <div class="relative">
          <i class="fas fa-bell text-gray-600 text-lg"></i>
          <span class="absolute -top-1 -right-2 bg-red-500 text-white text-xs font-bold px-1.5 py-0.5 rounded-full">3</span>
        </div> --}}

        <!-- User Menu -->
        <div class="flex items-center space-x-3">
          <div class="w-12 h-12 rounded-full bg-blue-600 text-white flex items-center justify-center text-sm shadow">
            {{-- <i class="fas fa-user"></i> --}}
            <ion-icon name="logo-octocat" class="text-3xl"></ion-icon>
          </div>
          <div class="text-md">
            <div class="font-semibold">{{ auth.user.fullName }}</div>
            <div class="text-xs text-gray-500 capitalize">{{ auth.user.role }}</div>
          </div>

          <form action="{{ route('auth.logout') }}" method="POST">
            {{ csrfField() }}
            <button type="submit" class="ml-3 text-red-600 hover:text-red-700 p-2 rounded-lg hover:bg-red-50 transition-all">
              <ion-icon name="log-out-outline" class="text-2xl"></ion-icon>
              {{-- <i class="fas fa-share-square"></i> --}}
            </button>
          </form>
        </div>
      </div>
    </header>

    <!-- Content Area -->
    <main class="p-6 overflow-y-auto flex-1 bg-gray-50">
      <!-- Flash Messages -->
      @if(flashMessages.has('success'))
        <div class="mb-6 bg-green-50 border border-green-200 text-green-700 px-4 py-3 rounded-lg fade-in">
          <i class="fas fa-check-circle mr-2"></i>
          {{ flashMessages.get('success') }}
        </div>
      @endif

      @if(flashMessages.has('error'))
        <div class="mb-6 bg-red-50 border border-red-200 text-red-700 px-4 py-3 rounded-lg fade-in">
          <i class="fas fa-exclamation-triangle mr-2"></i>
          {{ flashMessages.get('error') }}
        </div>
      @endif

      {{-- @section('title')
      Postes
      @endsection

      @section('content') --}}
      <!-- Premium Page Header with Animated Background -->
      <div class="relative bg-gradient-to-br from-purple-600/90 via-indigo-700/90 to-blue-800/90 rounded-3xl overflow-hidden mb-8 shadow-2xl"
          style="background-image: url('https://images.pexels.com/photos/3184291/pexels-photo-3184291.jpeg?auto=compress&cs=tinysrgb&w=1260&h=750&dpr=1'); background-size: cover; background-position: center; background-blend-mode: overlay;">
          <div class="absolute inset-0 bg-gradient-to-r from-cyan-950 to-orange-950/95"></div>

        <!-- Animated Background Elements -->
        <div class="absolute inset-0">
          <div class="absolute top-0 right-0 w-96 h-96 bg-white/10 rounded-full blur-3xl animate-pulse"></div>
          {{-- <div class="absolute bottom-0 left-0 w-80 h-80 bg-purple-300/20 rounded-full blur-2xl animate-bounce" style="animation-duration: 4s;"></div> --}}
          <div class="absolute top-1/3 left-1/3 w-64 h-64 bg-indigo-200/5 rounded-full blur-xl animate-pulse" style="animation-delay: 1s;"></div>
        </div>

        <!-- Animated Background Elements -->
        <div class="absolute inset-0">
          <div class="absolute top-0 right-0 w-96 h-96 bg-white/10 rounded-full blur-3xl animate-pulse"></div>
          {{-- <div class="absolute bottom-0 left-0 w-80 h-80 bg-purple-300/20 rounded-full blur-2xl animate-bounce" style="animation-duration: 4s;"></div> --}}
          <div class="absolute top-1/3 left-1/3 w-64 h-64 bg-indigo-200/5 rounded-full blur-xl animate-pulse" style="animation-delay: 1s;"></div>
        </div>

        <div class="relative z-10 p-10">
          <div class="flex items-center gap-8">
            {{-- <div class="bg-white/20 backdrop-blur-lg p-6 rounded-3xl shadow-xl border border-white/30">
              <i class="fas fa-file-invoice text-white text-6xl drop-shadow-lg"></i>
            </div> --}}
            <div>
              <h1 class="text-3xl font-bold text-white mb-3 text-shadow-lg">
                Générer Fiche de Paie
              </h1>
              <p class="text-teal-50 text-lg font-normal">
                Création de bulletin de salaire mensuel
              </p>
              <div class="flex items-center gap-6 mt-4">
                <div class="flex items-center gap-2">
                  <span class="text-teal-50 text-md">Calculs automatiques</span>
                </div>
                <div class="flex items-center gap-2">
                  <div class="w-3 h-3 bg-teal-50 rounded-full animate-pulse" style="animation-delay: 0.5s;"></div>
                  <span class="text-teal-50 text-md ml-3">Génération PDF des bulletins employés</span>
                </div>
              </div>
            </div>
          </div>
        </div>
      </div>

      <!-- Premium Form Container -->
      <div class="bg-white/95 backdrop-blur-sm rounded-3xl shadow-2xl border border-gray-200/50 overflow-hidden">
        <!-- Form Header -->
        {{-- <div class="bg-gradient-to-r from-purple-50 to-indigo-50 p-8 border-b border-gray-200/50">
          <div class="flex items-center justify-between">
            <div>
              <h2 class="text-2xl font-bold text-gray-800 mb-2">Paramètres de Génération</h2>
              <p class="text-gray-600">Sélectionnez l'employé et la période pour générer la fiche de paie</p>
            </div>
            <div class="bg-white/80 backdrop-blur-sm rounded-2xl p-4 shadow-lg">
              <div class="flex items-center gap-3">
                <div class="w-10 h-10 bg-gradient-to-r from-purple-500 to-indigo-500 rounded-xl flex items-center justify-center">
                  <i class="fas fa-calculator text-white"></i>
                </div>
                <div>
                  <div class="text-sm font-semibold text-gray-800">Nouvelle Fiche</div>
                  <div class="text-xs text-gray-500">Génération automatique</div>
                </div>
              </div>
            </div>
          </div>
        </div> --}}

        <div class="p-10" style="margin-top: -1%;">
          <form action="{{ route('pay_slips.store') }}" method="POST" class="space-y-10" id="payslipForm">
            {{ csrfField() }}

            <!-- Employee Selection Section -->
            <div class="form-section">
              <div class="flex items-center gap-4 mb-8">
                <div class="bg-gradient-to-r from-teal-500 to-teal-600 p-1 rounded-2xl shadow-lg">
                  {{-- <i class="fas fa-users text-white text-xl"></i> --}}
                </div>
                <div>
                  <h3 class="text-xl font-semibold text-gray-800">Sélection de l'Employé</h3>
                  {{-- <p class="text-gray-600">Choisissez l'employé pour lequel générer la fiche de paie</p> --}}
                </div>
              </div>

              <!-- Search and Filter Controls -->
              <div class="grid grid-cols-1 lg:grid-cols-3 gap-6 mb-6">
                <div class="form-group">
                  <label for="search_employee" class="form-label">
                    {{-- <i class="fas fa-search text-blue-500 mr-2"></i> --}}
                    Rechercher par Nom
                  </label>
                  <input type="text" id="search_employee" class="mt-2 w-full bg-gray-100 border border-gray-200 rounded-xl px-4 py-4 text-black-800 placeholder-black-700 focus:outline-none text-black-800 text-md focus:border-blue-400 focus:ring-2 focus:ring-blue-400/50 transition-all backdrop-blur-sm" placeholder="Tapez le nom de l'employé..." onkeyup="filterEmployees()">
                  {{-- <div class="form-hint">Recherche en temps réel dans la liste</div> --}}
                </div>

                <div class="form-group">
                  <label for="filter_department" class="form-label">
                    {{-- <i class="fas fa-building text-blue-500 mr-2"></i> --}}
                    Filtrer par Département
                  </label>
                  <select id="filter_department" class="mt-2 w-full bg-gray-100 border border-gray-200 rounded-xl px-4 py-4 text-black-800 placeholder-black-700 focus:outline-none text-black-800 text-md focus:border-blue-400 focus:ring-2 focus:ring-blue-400/50 transition-all backdrop-blur-sm" onchange="filterEmployees()">
                    <option value="">Tous les départements</option>
                    <option value="Direction">Direction</option>
                    <option value="Administration">Administration</option>
                    <option value="Marketing et Communication">Marketing et Communication</option>
                    <option value="Commercial">Commercial</option>
                    <option value="Technique et développement">Technique et développement</option>
                    <option value="Comptabilité finances">Comptabilité finances</option>
                  </select>
                  {{-- <div class="form-hint">Filtrer la liste par département</div> --}}
                </div>

                <div class="form-group">
                  <label for="sort_employees" class="form-label">
                    {{-- <i class="fas fa-sort text-blue-500 mr-2"></i> --}}
                    Trier par
                  </label>
                  <select id="sort_employees" class="mt-2 w-full bg-gray-100 border border-gray-200 rounded-xl px-4 py-4 text-black-800 placeholder-black-700 focus:outline-none text-black-800 text-md focus:border-blue-400 focus:ring-2 focus:ring-blue-400/50 transition-all backdrop-blur-sm" onchange="sortEmployees()">
                    <option value="name">Nom (A-Z)</option>
                    <option value="name_desc">Nom (Z-A)</option>
                    <option value="salary">Salaire (croissant)</option>
                    <option value="salary_desc">Salaire (décroissant)</option>
                    <option value="department">Département</option>
                  </select>
                  {{-- <div class="form-hint">Ordre d'affichage de la liste</div> --}}
                </div>
              </div>

              <!-- Employee Selection -->
              <div class="form-group">
                <label for="employee_id" class="form-label">
                  {{-- <i class="fas fa-user-tag text-blue-500 mr-2"></i> --}}
                  Employé Sélectionné
                </label>
                <select id="employee_id" name="employee_id" class="mt-2 w-full bg-gray-100 border border-gray-200 rounded-xl px-4 py-4 text-black-800 placeholder-black-700 focus:outline-none text-black-800 text-md focus:border-blue-400 focus:ring-2 focus:ring-blue-400/50 transition-all backdrop-blur-sm" required>
                  <option value="">Sélectionner un employé</option>
                  @each(employee in employees)
                    <option value="{{ employee.id }}"
                            data-name="{{ employee.fullName }}"
                            data-department="{{ employee.departement }}"
                            data-salary="{{ employee.salaire }}"
                            data-post="{{ employee.poste }}"
                            class="employee-option">
                      👤 {{ employee.fullName }} - {{ employee.poste }} - {{ employee.departement }} ({{ employee.salaire.toLocaleString() }} FCFA)
                    </option>
                  @endeach
                </select>
                {{-- <div class="form-hint">Employé pour lequel générer la fiche de paie</div> --}}

                <!-- Selected Employee Info Display -->
                {{-- <div id="selectedEmployeeInfo" class="hidden mt-4 p-6 bg-gradient-to-r from-blue-50 to-indigo-50 rounded-xl border border-blue-200">
                  <div class="flex items-center gap-4 mb-4">
                    <div class="w-3 h-3 bg-blue-500 rounded-full flex items-center justify-center">
                      {{-- <i class="fas fa-user text-white text-2xl"></i> --}}
                    </div>
                    <div>
                      <div class="font-bold text-xl text-blue-800" id="selectedEmployeeName">--</div>
                      <div class="text-blue-600" id="selectedEmployeePost">--</div>
                      <div class="text-sm text-blue-500" id="selectedEmployeeDept">--</div>
                    </div>
                  </div>
                  <div class="grid grid-cols-3 gap-4">
                    <div class="text-center p-3 bg-white/50 rounded-lg">
                      <div class="text-sm text-gray-600">Salaire Brut</div>
                      <div class="font-bold text-green-600" id="selectedEmployeeSalary">--</div>
                    </div>
                    <div class="text-center p-3 bg-white/50 rounded-lg">
                      <div class="text-sm text-gray-600">Cotisations (31,5%)</div>
                      <div class="font-bold text-red-600" id="calculatedCotisations">--</div>
                    </div>
                    <div class="text-center p-3 bg-white/50 rounded-lg">
                      <div class="text-sm text-gray-600">Salaire Net</div>
                      <div class="font-bold text-blue-600" id="calculatedNetSalary">--</div>
                    </div>
                  </div>
                </div> --}}
              </div>
            </div>

            <!-- Period Selection Section -->
            <div class="form-section">
              <div class="flex items-center gap-4 mb-8">
                <div class="bg-gradient-to-r from-green-500 to-emerald-600 p-1 rounded-2xl shadow-lg">
                  {{-- <i class="fas fa-calendar-alt text-white text-xl"></i> --}}
                </div>
                <div>
                  <h3 class="text-xl font-semibold text-gray-800">Période de Paie</h3>
                  {{-- <p class="text-gray-600">Définissez le mois et l'année pour la fiche de paie</p> --}}
                </div>
              </div>

              <div class="grid grid-cols-1 lg:grid-cols-2 gap-8">
                <div class="form-group">
                  <label for="mois" class="form-label">
                    {{-- <i class="fas fa-calendar text-green-500 mr-2"></i> --}}
                    Mois de Paie
                  </label>
                  <select id="mois" name="mois" class="mt-2 w-full bg-gray-100 border border-gray-200 rounded-xl px-4 py-4 text-black-800 placeholder-black-700 focus:outline-none text-black-800 text-md focus:border-blue-400 focus:ring-2 focus:ring-blue-400/50 transition-all backdrop-blur-sm" required>
                    <option value="">Sélectionner un mois</option>
                    <option value="Janvier">📅 Janvier</option>
                    <option value="Février">📅 Février</option>
                    <option value="Mars">📅 Mars</option>
                    <option value="Avril">📅 Avril</option>
                    <option value="Mai">📅 Mai</option>
                    <option value="Juin">📅 Juin</option>
                    <option value="Juillet">📅 Juillet</option>
                    <option value="Août">📅 Août</option>
                    <option value="Septembre">📅 Septembre</option>
                    <option value="Octobre">📅 Octobre</option>
                    <option value="Novembre">📅 Novembre</option>
                    <option value="Décembre">📅 Décembre</option>
                  </select>
                  {{-- <div class="form-hint">Mois pour lequel générer la fiche de paie</div> --}}
                </div>

                <div class="form-group">
                  <label for="annee" class="form-label">
                    {{-- <i class="fas fa-calendar-alt text-green-500 mr-2"></i> --}}
                    Année de Paie
                  </label>
                  <select id="annee" name="annee" class="mt-2 w-full bg-gray-100 border border-gray-200 rounded-xl px-4 py-4 text-black-800 placeholder-black-700 focus:outline-none text-black-800 text-md focus:border-blue-400 focus:ring-2 focus:ring-blue-400/50 transition-all backdrop-blur-sm" required>
                    <option value="">Sélectionner une année</option>
                    <option value="2028">🗓️ 2028</option>
                    <option value="2027">🗓️ 2027</option>
                    <option value="2026">🗓️ 2026</option>
                    <option value="2025">🗓️ 2025</option>
                    <option value="2024">🗓️ 2024</option>
                    <option value="2023">🗓️ 2023</option>
                  </select>
                  {{-- <div class="form-hint">Année pour laquelle générer la fiche de paie</div> --}}
                </div>
              </div>

              <!-- Period Summary -->
              <div id="periodSummary" class="hidden mt-6 p-4 bg-green-50 rounded-xl border border-green-200">
                <div class="flex items-center gap-3">
                  {{-- <i class="fas fa-info-circle text-green-600"></i> --}}
                  <div>
                    <div class="font-semibold text-green-800">Période sélectionnée</div>
                    <div class="text-green-700" id="selectedPeriod">--</div>
                  </div>
                </div>
              </div>
            </div>

            <!-- Calculation Preview Section -->
            {{-- <div class="form-section">
              <div class="flex items-center gap-4 mb-8">
                <div class="bg-gradient-to-r from-orange-500 to-red-600 p-1 rounded-2xl shadow-lg">
                  <i class="fas fa-calculator text-white text-xl"></i>
                </div>
                <div>
                  <h3 class="text-2xl font-bold text-gray-800">Aperçu des Calculs</h3>
                  <p class="text-gray-600 mt-2">Prévisualisation des montants avant génération</p>
                </div>
              </div>

              <div id="calculationPreview" class="hidden">
                <div class="grid grid-cols-1 md:grid-cols-3 gap-6 mb-6">
                  <div class="bg-gradient-to-br from-green-50 to-emerald-50 rounded-xl p-6 border border-green-200">
                    <div class="flex items-center justify-between mb-4">
                      <div class="bg-green-500 p-3 rounded-xl">
                        <i class="fas fa-money-bill-wave text-white text-xl"></i>
                      </div>
                      <div class="text-right">
                        <div class="text-sm text-gray-600">Salaire Brut</div>
                        <div class="text-2xl font-bold text-green-600" id="previewBrutSalary">0 FCFA</div>
                      </div>
                    </div>
                    <div class="text-sm text-green-700">Salaire mensuel de base</div>
                  </div>

                  <div class="bg-gradient-to-br from-red-50 to-pink-50 rounded-xl p-6 border border-red-200">
                    <div class="flex items-center justify-between mb-4">
                      <div class="bg-red-500 p-3 rounded-xl">
                        <i class="fas fa-minus-circle text-white text-xl"></i>
                      </div>
                      <div class="text-right">
                        <div class="text-sm text-gray-600">Cotisations</div>
                        <div class="text-2xl font-bold text-red-600" id="previewCotisations">0 FCFA</div>
                      </div>
                    </div>
                    <div class="text-sm text-red-700">31,5% du salaire brut</div>
                  </div>

                  <div class="bg-gradient-to-br from-blue-50 to-indigo-50 rounded-xl p-6 border border-blue-200">
                    <div class="flex items-center justify-between mb-4">
                      <div class="bg-blue-500 p-3 rounded-xl">
                        <i class="fas fa-coins text-white text-xl"></i>
                      </div>
                      <div class="text-right">
                        <div class="text-sm text-gray-600">Salaire Net</div>
                        <div class="text-2xl font-bold text-blue-600" id="previewNetSalary">0 FCFA</div>
                      </div>
                    </div>
                    <div class="text-sm text-blue-700">Montant à verser</div>
                  </div>
                </div>

                <!-- Calculation Details -->
                <div class="bg-gray-50 rounded-xl p-6">
                  <h4 class="font-semibold text-gray-800 mb-4">Détail des Calculs</h4>
                  <div class="space-y-3 text-sm">
                    <div class="flex justify-between items-center">
                      <span class="text-gray-600">Salaire brut mensuel:</span>
                      <span class="font-medium" id="detailBrutSalary">0 FCFA</span>
                    </div>
                    <div class="flex justify-between items-center">
                      <span class="text-gray-600">Cotisations sociales (31,5%):</span>
                      <span class="font-medium text-red-600" id="detailCotisations">- 0 FCFA</span>
                    </div>
                    <div class="border-t border-gray-300 pt-3 flex justify-between items-center">
                      <span class="font-semibold text-gray-800">Salaire net à payer:</span>
                      <span class="font-bold text-lg text-blue-600" id="detailNetSalary">0 FCFA</span>
                    </div>
                  </div>
                </div>
              </div>

              <div id="noCalculation" class="text-center py-8 text-gray-500">
                <i class="fas fa-calculator text-4xl mb-4"></i>
                <p>Sélectionnez un employé et une période pour voir l'aperçu des calculs</p>
              </div>
            </div> --}}

            <!-- Action Buttons -->
            <div class="flex justify-between items-center pt-8 border-t border-gray-200">
              <a href="{{ route('pay_slips.index') }}" class="bg-gray-700 hover:bg-cyan-50 text-white hover:text-black border border-gray-200 rounded-lg p-3 text-center transition-colors card-hover">
                {{-- <i class="fas fa-arrow-left mr-2"></i> --}}
                Retour aux Fiches
              </a>

              <div class="flex gap-4">
                <button type="button" class="bg-blue-50 hover:bg-cyan-50 border border-blue-100 rounded-lg p-3 text-center transition-colors card-hover" onclick="resetForm()">
                  {{-- <i class="fas fa-undo mr-2"></i> --}}
                  Réinitialiser
                </button>
                {{-- <button type="button" class="bg-blue-50 hover:bg-cyan-50 border border-blue-100 rounded-lg p-3 text-center transition-colors card-hover" onclick="previewPayslip()" id="previewBtn" disabled>
                  {{-- <i class="fas fa-eye mr-2"></i> --}}
                  Aperçu Complet
                </button> --}}
                <button type="submit" class="bg-blue-700/85 hover:bg-blue-50 text-white hover:text-black border border-blue-200 rounded-lg p-3 text-center transition-colors card-hover" id="generateBtn">
                  {{-- <i class="fas fa-file-invoice mr-2"></i> --}}
                  Générer la Fiche
                  <div class="btn-shine"></div>
                </button>
              </div>
            </div>
          </form>
        </div>
      </div>

      <!-- Preview Modal -->
      <div id="previewModal" class="fixed inset-0 bg-black/50 backdrop-blur-sm hidden items-center justify-center z-50">
        <div class="bg-white rounded-3xl shadow-2xl max-w-2xl w-full mx-4 max-h-[90vh] overflow-y-auto">
          <div class="p-8">
            <div class="flex items-center justify-between mb-6">
              <h3 class="text-2xl font-bold text-gray-800">Aperçu de la Fiche de Paie</h3>
              <button onclick="closePreview()" class="text-gray-500 hover:text-gray-700">
                <i class="fas fa-times text-xl"></i>
              </button>
            </div>
            <div id="previewContent" class="space-y-4">
              <!-- Preview content will be inserted here -->
            </div>
          </div>
        </div>
      </div>

      <style>
      /* Enhanced Form Styles */
      .form-group {
        @apply relative;
      }

      .form-label {
        @apply block text-sm font-semibold text-gray-700 mb-3 flex items-center;
      }

      .form-input, .form-select {
        @apply w-full bg-white border-2 border-gray-200 rounded-xl px-4 py-4 text-gray-800 placeholder-gray-400 focus:outline-none focus:border-blue-500 focus:ring-4 focus:ring-blue-100 transition-all duration-300 shadow-sm hover:border-gray-300;
      }

      .form-hint {
        @apply text-xs text-gray-500 mt-2 italic;
      }

      .form-section {
        @apply bg-gradient-to-br from-gray-50/80 to-white rounded-2xl p-8 border border-gray-100 shadow-sm;
      }

      .btn-secondary {
        @apply px-6 py-3 border-2 border-gray-300 rounded-xl text-gray-700 font-semibold hover:bg-gray-50 hover:border-gray-400 transition-all duration-200 flex items-center;
      }

      .btn-outline {
        @apply px-6 py-3 border-2 border-blue-300 rounded-xl text-blue-600 font-semibold hover:bg-blue-50 hover:border-blue-400 transition-all duration-200 flex items-center;
      }

      .btn-preview {
        @apply px-6 py-3 border-2 border-purple-300 rounded-xl text-purple-600 font-semibold hover:bg-purple-50 hover:border-purple-400 transition-all duration-200 flex items-center disabled:opacity-50 disabled:cursor-not-allowed;
      }

      .btn-primary-gradient {
        @apply relative px-8 py-4 bg-gradient-to-r from-purple-600 to-indigo-600 hover:from-purple-700 hover:to-indigo-700 text-white font-bold rounded-xl transition-all duration-300 transform hover:scale-105 shadow-lg hover:shadow-xl flex items-center overflow-hidden disabled:opacity-50 disabled:cursor-not-allowed disabled:transform-none;
      }

      .btn-shine {
        @apply absolute inset-0 bg-gradient-to-r from-transparent via-white/20 to-transparent transform -skew-x-12 -translate-x-full transition-transform duration-1000;
      }

      .btn-primary-gradient:hover .btn-shine {
        @apply translate-x-full;
      }

      /* Form Enhancement Effects */
      .form-input:focus, .form-select:focus {
        transform: translateY(-2px);
        box-shadow: 0 10px 25px -5px rgba(59, 130, 246, 0.315);
      }

      /* Section Animation */
      .form-section {
        animation: slideInUp 0.6s ease-out;
      }

      @keyframes slideInUp {
        from {
          opacity: 0;
          transform: translateY(30px);
        }
        to {
          opacity: 1;
          transform: translateY(0);
        }
      }
      </style>

      <script>
      let employees = @json(employees);

      // Employee filtering and sorting
      function filterEmployees() {
        const searchTerm = document.getElementById('search_employee').value.toLowerCase();
        const departmentFilter = document.getElementById('filter_department').value;
        const employeeSelect = document.getElementById('employee_id');

        // Clear current options except first one
        while (employeeSelect.children.length > 1) {
          employeeSelect.removeChild(employeeSelect.lastChild);
        }

        // Filter employees
        let filteredEmployees = employees.filter(employee => {
          const matchesSearch = employee.fullName.toLowerCase().includes(searchTerm);
          const matchesDepartment = !departmentFilter || employee.departement === departmentFilter;
          return matchesSearch && matchesDepartment;
        });

        // Add filtered options
        filteredEmployees.forEach(employee => {
          const option = document.createElement('option');
          option.value = employee.id;
          option.setAttribute('data-name', employee.fullName);
          option.setAttribute('data-department', employee.departement);
          option.setAttribute('data-salary', employee.salaire);
          option.setAttribute('data-post', employee.poste);
          option.className = 'employee-option';
          option.textContent = `👤 ${employee.fullName} - ${employee.poste} - ${employee.departement} (${employee.salaire.toLocaleString()} FCFA)`;
          employeeSelect.appendChild(option);
        });
      }

      function sortEmployees() {
        const sortBy = document.getElementById('sort_employees').value;

        employees.sort((a, b) => {
          switch(sortBy) {
            case 'name':
              return a.fullName.localeCompare(b.fullName);
            case 'name_desc':
              return b.fullName.localeCompare(a.fullName);
            case 'salary':
              return a.salaire - b.salaire;
            case 'salary_desc':
              return b.salaire - a.salaire;
            case 'department':
              return a.departement.localeCompare(b.departement);
            default:
              return 0;
          }
        });

        filterEmployees();
      }

      // Employee selection handler
      document.getElementById('employee_id').addEventListener('change', function() {
        const selectedOption = this.options[this.selectedIndex];
        const employeeInfo = document.getElementById('selectedEmployeeInfo');

        if (this.value) {
          const employeeName = selectedOption.dataset.name;
          const employeePost = selectedOption.dataset.post;
          const employeeDept = selectedOption.dataset.department;
          const employeeSalary = parseInt(selectedOption.dataset.salary);

          // Update display
          document.getElementById('selectedEmployeeName').textContent = employeeName;
          document.getElementById('selectedEmployeePost').textContent = employeePost;
          document.getElementById('selectedEmployeeDept').textContent = employeeDept;
          document.getElementById('selectedEmployeeSalary').textContent = employeeSalary.toLocaleString() + ' FCFA';

          // Calculate cotisations and net salary
          const cotisations = Math.round(employeeSalary * 0.315);
          const netSalary = employeeSalary - cotisations;

          document.getElementById('calculatedCotisations').textContent = cotisations.toLocaleString() + ' FCFA';
          document.getElementById('calculatedNetSalary').textContent = netSalary.toLocaleString() + ' FCFA';

          employeeInfo.classList.remove('hidden');
          updateCalculationPreview();
        } else {
          employeeInfo.classList.add('hidden');
          hideCalculationPreview();
        }

        checkFormCompletion();
      });

      // Period selection handlers
      document.getElementById('mois').addEventListener('change', updatePeriodSummary);
      document.getElementById('annee').addEventListener('change', updatePeriodSummary);

      function updatePeriodSummary() {
        const mois = document.getElementById('mois').value;
        const annee = document.getElementById('annee').value;
        const periodSummary = document.getElementById('periodSummary');

        if (mois && annee) {
          document.getElementById('selectedPeriod').textContent = `${mois} ${annee}`;
          periodSummary.classList.remove('hidden');
        } else {
          periodSummary.classList.add('hidden');
        }

        updateCalculationPreview();
        checkFormCompletion();
      }

      function updateCalculationPreview() {
        const employeeSelect = document.getElementById('employee_id');
        const selectedOption = employeeSelect.options[employeeSelect.selectedIndex];
        const mois = document.getElementById('mois').value;
        const annee = document.getElementById('annee').value;

        if (employeeSelect.value && mois && annee) {
          const salary = parseInt(selectedOption.dataset.salary);
          const cotisations = Math.round(salary * 0.315);
          const netSalary = salary - cotisations;

          // Update preview
          document.getElementById('previewBrutSalary').textContent = salary.toLocaleString() + ' FCFA';
          document.getElementById('previewCotisations').textContent = cotisations.toLocaleString() + ' FCFA';
          document.getElementById('previewNetSalary').textContent = netSalary.toLocaleString() + ' FCFA';

          // Update details
          document.getElementById('detailBrutSalary').textContent = salary.toLocaleString() + ' FCFA';
          document.getElementById('detailCotisations').textContent = '- ' + cotisations.toLocaleString() + ' FCFA';
          document.getElementById('detailNetSalary').textContent = netSalary.toLocaleString() + ' FCFA';

          document.getElementById('calculationPreview').classList.remove('hidden');
          document.getElementById('noCalculation').classList.add('hidden');
        } else {
          hideCalculationPreview();
        }
      }

      function hideCalculationPreview() {
        document.getElementById('calculationPreview').classList.add('hidden');
        document.getElementById('noCalculation').classList.remove('hidden');
      }

      function checkFormCompletion() {
        const employeeSelected = document.getElementById('employee_id').value;
        const moisSelected = document.getElementById('mois').value;
        const anneeSelected = document.getElementById('annee').value;

        const isComplete = employeeSelected && moisSelected && anneeSelected;

        document.getElementById('previewBtn').disabled = !isComplete;
        document.getElementById('generateBtn').disabled = !isComplete;
      }

      function resetForm() {
        if (confirm('Êtes-vous sûr de vouloir réinitialiser le formulaire ?')) {
          document.getElementById('payslipForm').reset();
          document.getElementById('selectedEmployeeInfo').classList.add('hidden');
          document.getElementById('periodSummary').classList.add('hidden');
          hideCalculationPreview();
          checkFormCompletion();

          // Reset employee list
          filterEmployees();
        }
      }

      function previewPayslip() {
        const employeeSelect = document.getElementById('employee_id');
        const selectedOption = employeeSelect.options[employeeSelect.selectedIndex];
        const employeeName = selectedOption.dataset.name;
        const employeePost = selectedOption.dataset.post;
        const employeeDept = selectedOption.dataset.department;
        const salary = parseInt(selectedOption.dataset.salary);
        const mois = document.getElementById('mois').value;
        const annee = document.getElementById('annee').value;

        const cotisations = Math.round(salary * 0.315);
        const netSalary = salary - cotisations;

        const previewContent = `
          <div class="bg-gradient-to-r from-purple-50 to-indigo-50 rounded-xl p-6 mb-6">
            <div class="text-center mb-4">
              <h4 class="text-2xl font-bold text-gray-800">FICHE DE PAIE</h4>
              <p class="text-purple-700 font-medium">${mois} ${annee}</p>
            </div>

            <div class="grid grid-cols-2 gap-4 mb-6">
              <div>
                <h5 class="font-semibold text-gray-800 mb-2">EMPLOYÉ</h5>
                <div class="text-sm space-y-1">
                  <div><strong>Nom:</strong> ${employeeName}</div>
                  <div><strong>Poste:</strong> ${employeePost}</div>
                  <div><strong>Département:</strong> ${employeeDept}</div>
                </div>
              </div>
              <div>
                <h5 class="font-semibold text-gray-800 mb-2">ENTREPRISE</h5>
                <div class="text-sm space-y-1">
                  <div><strong>RELIANCE ESCALADE</strong></div>
                  <div>Lomé, Togo</div>
                  <div>Période: ${mois} ${annee}</div>
                </div>
              </div>
            </div>
          </div>

          <div class="bg-white border border-gray-200 rounded-xl overflow-hidden mb-6">
            <table class="w-full">
              <thead class="bg-gray-50">
                <tr>
                  <th class="px-4 py-3 text-left text-sm font-semibold text-gray-700">LIBELLÉ</th>
                  <th class="px-4 py-3 text-right text-sm font-semibold text-gray-700">MONTANT</th>
                </tr>
              </thead>
              <tbody class="divide-y divide-gray-200">
                <tr>
                  <td class="px-4 py-3 text-sm text-gray-900">Salaire de base</td>
                  <td class="px-4 py-3 text-sm text-right font-medium text-green-600">${salary.toLocaleString()} FCFA</td>
                </tr>
                <tr>
                  <td class="px-4 py-3 text-sm text-gray-900">Cotisations sociales (31,5%)</td>
                  <td class="px-4 py-3 text-sm text-right font-medium text-red-600">-${cotisations.toLocaleString()} FCFA</td>
                </tr>
                <tr class="bg-blue-50">
                  <td class="px-4 py-3 text-sm font-bold text-gray-900">SALAIRE NET À PAYER</td>
                  <td class="px-4 py-3 text-sm text-right font-bold text-blue-600">${netSalary.toLocaleString()} FCFA</td>
                </tr>
              </tbody>
            </table>
          </div>

          <div class="text-center text-xs text-gray-500">
            <p>Document généré automatiquement par le système RH RELIANCE ESCALADE</p>
            <p>Date de génération: ${new Date().toLocaleDateString('fr-FR')}</p>
          </div>
        `;

        document.getElementById('previewContent').innerHTML = previewContent;
        document.getElementById('previewModal').classList.remove('hidden');
        document.getElementById('previewModal').classList.add('flex');
      }

      function closePreview() {
        document.getElementById('previewModal').classList.add('hidden');
        document.getElementById('previewModal').classList.remove('flex');
      }

      // Set current month and year as default
      document.addEventListener('DOMContentLoaded', function() {
        const now = new Date();
        const currentMonth = now.toLocaleDateString('fr-FR', { month: 'long' });
        const currentYear = now.getFullYear();

        document.getElementById('mois').value = currentMonth.charAt(0).toUpperCase() + currentMonth.slice(1);
        document.getElementById('annee').value = currentYear.toString();

        updatePeriodSummary();
        checkFormCompletion();
      });
      </script>
      {{-- @endsection --}}
      </main>
  </div>
</body>
</html>
