<!DOCTYPE html>
<html lang="fr">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>@!section('title') - RELIANCE ESCALADE</title>
  <script src="https://cdn.tailwindcss.com"></script>
  <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css" rel="stylesheet">
  <link href="/public/css/app.css" rel="stylesheet">
</head>
<body class="bg-gray-100 min-h-screen text-gray-900 flex">

  <!-- Sidebar -->
  <aside class="w-64 bg-white border-r min-h-screen shadow-sm flex flex-col py-6 px-4">
    <div class="mb-10">
      <div class="flex items-center space-x-3">
        <div class="bg-blue-600 text-white rounded-lg p-2 font-bold text-sm w-9 h-9 flex items-center justify-center">RE</div>
        <div>
          <h1 class="text-lg font-semibold text-gray-800">RELIANCE ESCALADE</h1>
          <p class="text-xs text-gray-500">Gestion des Ressources Humaines</p>
        </div>
      </div>
    </div>

    <nav class="flex flex-col space-y-2 text-sm font-medium">
      <a href="{{ route('dashboard') }}" class="sidebar-link flex items-center px-3 py-2 rounded-lg hover:bg-gray-100 {{ request.url() === route('dashboard') ? 'bg-blue-100 text-blue-700 font-semibold' : '' }}">
        <i class="fas fa-th-large mr-3"></i> Dashboard
      </a>
      <a href="{{ route('posts.index') }}" class="sidebar-link flex items-center px-3 py-2 rounded-lg hover:bg-gray-100 {{ request.url().includes('/posts') ? 'bg-blue-100 text-blue-700 font-semibold' : '' }}">
        <i class="fas fa-briefcase mr-3"></i> Postes
      </a>
      <a href="{{ route('employees.index') }}" class="sidebar-link flex items-center px-3 py-2 rounded-lg hover:bg-gray-100 {{ request.url().includes('/employees') ? 'bg-blue-100 text-blue-700 font-semibold' : '' }}">
        <i class="fas fa-users mr-3"></i> Personnel
      </a>
      <a href="{{ route('promotions.index') }}" class="sidebar-link flex items-center px-3 py-2 rounded-lg hover:bg-gray-100 {{ request.url().includes('/promotions') ? 'bg-blue-100 text-blue-700 font-semibold' : '' }}">
        <i class="fas fa-arrow-up mr-3"></i> Promotions
      </a>
      <a href="{{ route('demotions.index') }}" class="sidebar-link flex items-center px-3 py-2 rounded-lg hover:bg-gray-100 {{ request.url().includes('/demotions') ? 'bg-blue-100 text-blue-700 font-semibold' : '' }}">
        <i class="fas fa-arrow-down mr-3"></i> Démotions
      </a>
      <a href="{{ route('pay_slips.index') }}" class="sidebar-link flex items-center px-3 py-2 rounded-lg hover:bg-gray-100 {{ request.url().includes('/pay-slips') ? 'bg-blue-100 text-blue-700 font-semibold' : '' }}">
        <i class="fas fa-file-invoice mr-3"></i> Fiches de Paie
      </a>
      @if(auth.user.role === 'Admin')
      <a href="{{ route('users.index') }}" class="sidebar-link flex items-center px-3 py-2 rounded-lg hover:bg-gray-100 {{ request.url().includes('/users') ? 'bg-blue-100 text-blue-700 font-semibold' : '' }}">
        <i class="fas fa-user-cog mr-3"></i> Utilisateurs
      </a>
      @endif
    </nav>
  </aside>

  <!-- Main Panel -->
  <div class="flex-1 flex flex-col">

    <!-- Header -->
    <header class="h-16 bg-white border-b px-6 flex items-center justify-between shadow-sm">
      <div></div> <!-- Espace vide pour futur breadcrumb -->

      <div class="flex items-center space-x-6">
        <!-- Notifications -->
        <div class="relative">
          <i class="fas fa-bell text-gray-600 text-lg"></i>
          <span class="absolute -top-1 -right-2 bg-red-500 text-white text-xs font-bold px-1.5 py-0.5 rounded-full">3</span>
        </div>

        <!-- User Menu -->
        <div class="flex items-center space-x-3">
          <div class="w-9 h-9 rounded-full bg-blue-600 text-white flex items-center justify-center text-sm shadow">
            <i class="fas fa-user"></i>
          </div>
          <div class="text-sm">
            <div class="font-semibold">{{ auth.user.fullName }}</div>
            <div class="text-xs text-gray-500 capitalize">{{ auth.user.role }}</div>
          </div>

          <form action="{{ route('auth.logout') }}" method="POST">
            {{ csrfField() }}
            <button type="submit" class="ml-3 text-red-600 hover:text-red-700 p-2 rounded-lg hover:bg-red-50 transition-all">
              <i class="fas fa-sign-out-alt"></i>
            </button>
          </form>
        </div>
      </div>
    </header>

    <!-- Content Area -->
    <main class="p-6 overflow-y-auto flex-1 bg-gray-50">
      <!-- Flash Messages -->
      @if(flashMessages.has('success'))
        <div class="mb-6 bg-green-50 border border-green-200 text-green-700 px-4 py-3 rounded-lg fade-in">
          <i class="fas fa-check-circle mr-2"></i>
          {{ flashMessages.get('success') }}
        </div>
      @endif

      @if(flashMessages.has('error'))
        <div class="mb-6 bg-red-50 border border-red-200 text-red-700 px-4 py-3 rounded-lg fade-in">
          <i class="fas fa-exclamation-triangle mr-2"></i>
          {{ flashMessages.get('error') }}
        </div>
      @endif

      @!section('content')
    </main>
  </div>
</body>
</html>







{{-- <!DOCTYPE html>
<html lang="fr">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>@!section('title') - RELIANCE ESCALADE</title>
  <script src="https://cdn.tailwindcss.com"></script>
  <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css" rel="stylesheet">
  <link href="/public/css/app.css" rel="stylesheet">
</head>
<body class="bg-gray-50 min-h-screen">
  <!-- Header -->
  <header class="bg-white shadow-sm border-b border-gray-200 sticky top-0 z-50">
    <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
      <div class="flex justify-between items-center h-16">
        <!-- Logo -->
        <div class="flex items-center">
          <div class="gradient-bg p-2 rounded-lg shadow-md">
            <div class="w-8 h-8 bg-white rounded-sm flex items-center justify-center">
              <span class="text-blue-600 font-bold text-sm">RE</span>
            </div>
          </div>
          <div class="ml-4">
            <h1 class="text-xl font-bold text-gray-800">RELIANCE ESCALADE</h1>
            <p class="text-sm text-gray-600">Gestion des Ressources Humaines</p>
          </div>
        </div>

        <!-- User Menu -->
        <div class="flex items-center space-x-4">
          <div class="relative">
            <div class="flex items-center space-x-3 text-gray-700">
              <div class="gradient-bg p-2 rounded-full shadow-md">
                <i class="fas fa-user text-white text-sm"></i>
              </div>
              <div class="text-left">
                <p class="text-sm font-medium">{{ auth.user.fullName }}</p>
                <p class="text-xs text-gray-600 capitalize">{{ auth.user.role }}</p>
              </div>
            </div>
          </div>

          <form action="{{ route('auth.logout') }}" method="POST">
            {{ csrfField() }}
            <button type="submit" class="text-red-600 hover:text-red-700 p-2 rounded-lg hover:bg-red-50 transition-all">
              <i class="fas fa-sign-out-alt"></i>
            </button>
          </form>
        </div>
      </div>
    </div>
  </header>

  <div class="flex">
    <!-- Sidebar -->
    <aside class="w-64 bg-white shadow-sm min-h-screen border-r border-gray-200">
      <nav class="mt-8 px-4">
        <div class="space-y-2">
          <a href="{{ route('dashboard') }}" class="sidebar-link {{ request.url() === route('dashboard') ? 'active' : '' }}">
            <i class="fas fa-tachometer-alt mr-3"></i>
            Dashboard
          </a>

          <a href="{{ route('posts.index') }}" class="sidebar-link {{ request.url().includes('/posts') ? 'active' : '' }}">
            <i class="fas fa-briefcase mr-3"></i>
            Postes
          </a>

          <a href="{{ route('employees.index') }}" class="sidebar-link {{ request.url().includes('/employees') ? 'active' : '' }}">
            <i class="fas fa-users mr-3"></i>
            Personnel
          </a>

          <a href="{{ route('promotions.index') }}" class="sidebar-link {{ request.url().includes('/promotions') ? 'active' : '' }}">
            <i class="fas fa-arrow-up mr-3"></i>
            Promotions
          </a>

          <a href="{{ route('demotions.index') }}" class="sidebar-link {{ request.url().includes('/demotions') ? 'active' : '' }}">
            <i class="fas fa-arrow-down mr-3"></i>
            Démotions
          </a>

          <a href="{{ route('pay_slips.index') }}" class="sidebar-link {{ request.url().includes('/pay-slips') ? 'active' : '' }}">
            <i class="fas fa-file-invoice mr-3"></i>
            Fiches de Paie
          </a>

          @if(auth.user.role === 'Admin')
          <a href="{{ route('users.index') }}" class="sidebar-link {{ request.url().includes('/users') ? 'active' : '' }}">
            <i class="fas fa-user-cog mr-3"></i>
            Utilisateurs
          </a>
          @endif
        </div>
      </nav>
    </aside>

    <!-- Main Content -->
    <main class="flex-1 p-8">
      <!-- Flash Messages -->
      @if(flashMessages.has('success'))
        <div class="mb-6 bg-green-50 border border-green-200 text-green-700 px-4 py-3 rounded-lg fade-in">
          <i class="fas fa-check-circle mr-2"></i>
          {{ flashMessages.get('success') }}
        </div>
      @endif

      @if(flashMessages.has('error'))
        <div class="mb-6 bg-red-50 border border-red-200 text-red-700 px-4 py-3 rounded-lg fade-in">
          <i class="fas fa-exclamation-triangle mr-2"></i>
          {{ flashMessages.get('error') }}
        </div>
      @endif

      @!section('content')

    </main>
  </div>
</body>
</html> --}}
